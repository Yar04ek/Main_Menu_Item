import { jsx as _jsx } from "react/jsx-runtime";
import { PureComponent } from 'react';
import dataTests from '../global/data-tests';
import Tabs from './dumb-tabs';
export default class SmartTabs extends PureComponent {
    constructor(props) {
        super(props);
        this.state = {
            selected: this.props.initSelected || (Array.isArray(this.props.children) && this.props.children[0].props.id) || '0',
        };
    }
    state;
    handleSelect = (selected) => this.setState({ selected });
    render() {
        const { children, initSelected, 'data-test': dataTest, ...restProps } = this.props;
        return (_jsx(Tabs, { "data-test": dataTests('ring-smart-tabs', dataTest), selected: this.state.selected, onSelect: this.handleSelect, ...restProps, children: children }));
    }
}
