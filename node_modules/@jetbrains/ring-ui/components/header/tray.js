import { jsx as _jsx } from "react/jsx-runtime";
import { Children, Component } from 'react';
import classNames from 'classnames';
import styles from './header.css';
const wrapChild = (child) => child && _jsx("div", { className: styles.trayItem, children: child });
export default class Tray extends Component {
    render() {
        const { children, className, ...restProps } = this.props;
        const classes = classNames(styles.tray, className);
        return (_jsx("div", { ...restProps, className: classes, children: Children.map(children, wrapChild) }));
    }
}
